//swiftlint:disable force_unwrapping

{% macro recursiveBlock images %}
  {% for image in images %}
  {% if not image.items %}
        public static var {{image.value|swiftIdentifier|snakeToCamelCase|lowerFirstWord|escapeReservedKeywords}} : UIImage { return Bundle.core.image(named: "{{image.value}}")! }
  {% else %}
        public enum {{ image.name }} {
  {% call recursiveBlock image.items %}
        }
  {% endif %}
  {% endfor %}
{% endmacro %}
// autogenerated with SwiftGen
// DO NOT EDIT

extension Images {
{% for catalog in catalogs %}
{% call recursiveBlock catalog.assets %}
{% endfor %}
}

