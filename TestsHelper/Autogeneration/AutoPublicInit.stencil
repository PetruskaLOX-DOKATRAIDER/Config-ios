/*
import Core

{% for type in types.implementing.AutoInit|struct where type.accessLevel == "public" %}
public extension {{ type.name }} {
    public static func new() -> {{type.name}} {
        return {{type.name}}.new({% for argument in type.variables where not argument.isComputed %}{{argument.name}}: .new(){% if not forloop.last %}, {% endif %}{% endfor %})
    }
}

{% endfor %}
*/
